#include "ParserBase.h"

namespace parser {
/*    typedef ParserBase::Char Char;
    ParserBase::ParserBase(istream& _input)
        : input(_input)
    {}

    ParserBase::~ParserBase(){}
    
    Char ParserBase::cur(){
        if (!hasInBuffer)
            getOne();
            
        static_assert(bufPos>=0);
        static_assert(bufPos<buf.size());
        return buf[bufPos];
    }
    
    void ParserBase::getOne(){
        Char c;
        auto r = istream.get(&c);
        
    }*/
}
